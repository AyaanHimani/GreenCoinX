const express = require("express");
// const { allTxnProductions, blacklistConsumer } = require("../controllers/regulatorController")

const router = express.Router();

// router.get("/allTransactionAndProduction", allTxnProductions);
// router.put("/blacklist", blacklistConsumer);

module.exports = router;